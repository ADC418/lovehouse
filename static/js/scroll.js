   // 楼梯导航
   //    var item = document.querySelectorAll('.box')
   //        // 获取浏览器窗口的宽高
   //    var wHeight = window.innerHeight
   //        // 获取每一个导航项的偏移值
   //        //    获取导航条的滚动值,
   //        //    也就是在窗口页面被卷出的高度
   //    var index = 0
   //    onmousewheel = function(e) {

   //        var stop = document.documentElement.scrollTop;
   //        if (e.wheelDelta === -120) {
   //            index++
   //            stop = index * 956
   //            console.log(stop)
   //            for (var i = 0; i < item.length; i++) {
   //                var offset = item[i].offsetTop
   //                if (offset === 956) {
   //                    console.log(111)
   //                }
   //            }
   //        }


   //    }